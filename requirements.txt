# FastAPI and server
fastapi>=0.104.0,<0.110.0
uvicorn[standard]>=0.24.0,<0.30.0

# Data validation
pydantic>=2.0.0,<3.0.0

# Geographic utilities
pycountry-convert>=0.7.2  # For country to continent code conversion (optional - has manual fallback)
shapely>=2.0.0  # For geometric operations (intersection detection, buffering)

# HTTP requests
requests>=2.31.0  # For API calls (OSM Overpass API, geocoding services)

# AI/LLM integration
openai>=1.0.0  # For Groq API integration (market oracle service)

# Environment variables
python-dotenv>=1.0.0  # For loading .env file

# Optional: For better JSON serialization (if needed)
# python-multipart>=0.0.6  # Required if using form data



